# Realer Schreibbeleg F11 WS-3 AK8(b2)

Dedizierte, dauerhaft getrackte Datei für den zweiten Teil des
schreibenden AK8-Nachweises (`features/F11/feature.md`,
`state/e2e-nachweis-f11-ws3.md`) — bewusst getrennt von der
Nachweisdatei selbst (Reviewer-/QA-Hinweis nach dem ersten Durchlauf:
eine eigens getrennte Zieldatei ist eindeutiger als ein Anhang an das
Nachweisdokument, das den Lauf zugleich beschreibt). Diese Datei trägt
keinen anderen Zweck als diesen Beleg.

Die folgende Zeile wurde real von einem echten Claude-Code-Kindprozess
über den Leitstand-Schreibpfad (`POST /api/laeufe`, Werkzeugsatz
`schreibend`) eingefügt, nicht von der bauenden Sitzung:

<!-- F11-WS3-B2-MARKER -->
F11-WS3-B2-BELEG: f01bc8b5-fc38-44f5-b27f-d1313b68946b
